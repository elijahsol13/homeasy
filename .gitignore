# Dependencies
node_modules/

# Build output
dist/

# Environment secrets & keys
.env
.env.*
*.pem
deploy.sh

# Temporary / sensitive data files
data/*.db-wal
data/*.db-shm
data/fb_session.json
data/k24_session.json
INFRASTRUCTURE_SETUP.md

# Logs
*.log

# OS & IDE artifacts
.DS_Store
Thumbs.db
.nosync
*.nosync
.vscode/
.idea/

# Tool & agent artifacts
repomix-output.xml
BACKLOG.md
*plan*.md
*PLAN*.md
TMA_*.md
walkthrough*.md
task*.md
scratch/

# Database and backups
data/homeasy.db
data/backups/
backups/
*.tar.gz
*.zip
data/reparse_checkpoint.json
data/ai_extraction_eval.json
data/discovered_features_report.json

# Archived/legacy one-off debug & PoC scripts (kept locally, not shipped)
scripts/_archive/

# Persistent Playwright disk cache for proxy bandwidth conservation — KEEP ON DISK,
# never delete this folder, just don't track it in git (large, binary, host-specific).
data/browser_cache/

# Scraper debug dumps & runtime artifacts (never source of truth, regenerated on each run)
data/*.html
data/fb_js/
data/fb_error_dumps/
data/fb_debug_resp.json
data/fb_doc_id.txt
data/fb_groups_state.json
data/scraper_state.json
data/database.sqlite
data/homeasy_before.db*
data/homeasy_backup_final.db
js_urls.txt
scratch.html

